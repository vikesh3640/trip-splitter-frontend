"use client";

export default function AddTransactionFab() {
  return (
    <button
      className="fab"
      title="Add Transaction"
      onClick={() => alert("Add Transaction form coming next")}
    >
      Add Transaction
    </button>
  );
}
